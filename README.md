# Gonka Ansible Playbook

```
cp hosts.example hosts
```

# Edit the hosts file with the correct IP address and SSH key information

Run the playbook step by step to ensure everything is working correctly:
```
ansible-playbook playbook.yml --step
```
